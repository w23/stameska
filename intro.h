/* File generated with Shader Minifier 1.1.4
 * http://www.ctrl-alt-test.fr
 */
#ifndef INTRO_H_
# define INTRO_H_

const char *intro_glsl =
 "#version 130\n"
 "uniform int s;"
 "uniform sampler2D T;"
 "vec2 v=vec2(640.,480.);"
 "float m=float(s)/44096.;"
 "const vec3 i=vec3(0.,.001,1.);"
 "const float x=3.14159;"
 "float f=m/8.,c=m/16.,y=m/32.,r=m/64.;"
 "float n(float v)"
 "{"
   "return fract(sin(v)*43758.5);"
 "}"
 "float t(vec2 v)"
 "{"
   "return n(dot(v,vec2(129.47,37.56)));"
 "}"
 "float h(vec2 v)"
 "{"
   "vec2 m=floor(v);"
   "v=fract(v);"
   "return mix(mix(t(m),t(m+i.zx),v.x),mix(t(m+i.xz),t(m+i.zz),v.x),v.y);"
 "}"
 "float d(vec2 v)"
 "{"
   "return.5*h(v)+.25*h(v*2.3)+.125*h(v*4.1);"
 "}"
 "float d(vec2 v,vec2 m)"
 "{"
   "return v=abs(v)-m,max(v.x,v.y);"
 "}"
 "float h(vec3 v,vec3 m)"
 "{"
   "return v=abs(v)-m,max(v.x,max(v.y,v.z));"
 "}"
 "float n(inout vec2 v,float m)"
 "{"
   "float f=2*x/m,y=atan(v.y,v.x)+f/2.,g=length(v),i=floor(y/f);"
   "y=mod(y,f)-f/2.;"
   "v=vec2(cos(y),sin(y))*g;"
   "if(abs(i)>=m/2)"
     "i=abs(i);"
   "return i;"
 "}"
 "vec3 t(vec3 v,vec3 m)"
 "{"
   "return mod(v,m)-m*.5;"
 "}"
 "vec2 p(vec2 v,vec2 m)"
 "{"
   "return mod(v,m)-m*.5;"
 "}"
 "float d(vec3 v,float m,float i,float y)"
 "{"
   "return max(abs(v.y)-y,max(length(v)-i,m-length(v)));"
 "}"
 "float e(vec3 v,vec2 m)"
 "{"
   "return length(vec2(length(v.xz)-m.x,v.y))-m.y;"
 "}"
 "mat3 e(float v)"
 "{"
   "float m=sin(v),y=cos(v);"
   "return mat3(1.,0.,0.,0.,y,-m,0.,m,y);"
 "}"
 "mat3 p(float v)"
 "{"
   "float m=sin(v),y=cos(v);"
   "return mat3(y,0.,m,0.,1.,0,-m,0.,y);"
 "}"
 "mat3 a(float v)"
 "{"
   "float m=sin(v),y=cos(v);"
   "return mat3(y,m,0.,-m,y,0.,0.,0.,1.);"
 "}"
 "float a(float v,float m,float y)"
 "{"
   "return max(y,min(v,m))-length(max(vec2(y-v,y-m),vec2(0)));"
 "}"
 "float l(vec3 v)"
 "{"
   "return dot(v,v);"
 "}"
 "float a(vec3 m,vec3 y)"
 "{"
   "float f=4./v.x;"
   "return max(h(m,y),-min(min(h(m,y-vec3(-1.,f,f)),h(m,y-vec3(f,-1.,f))),h(m,y-vec3(f,f,-1.))));"
 "}"
 "int g=0;"
 "float z=1.,u=1.;"
 "float D(vec3 v)"
 "{"
   "if(g==1)"
     "{"
       "vec3 x=a(m/10.)*p(m/20.)*(v+1.5*i.zxx*sin(y)),k=a(m/17.)*p(m/23.)*(v-1.5*i.zxx*sin(y));"
       "z=min(a(x,vec3(1.)),a(k,vec3(1.)));"
       "u=max(h(x,vec3(.9)),h(k,vec3(.9)));"
       "return min(z,u);"
     "}"
   "else"
     " if(g==3)"
       "return length(v)-1.;"
     "else"
       " if(g==4)"
         "{"
           "float r=0.;"
           "for(int x=0;x<4;++x)"
             "{"
               "float k=float(x)*4.;"
               "vec3 t=v-1.5*vec3(cos(c+k),sin(f+k*3.),cos(y+k));"
               "r+=.5/dot(t,t);"
             "}"
           "return 1.-r;"
         "}"
       "else"
         " if(g==5)"
           "{"
             "vec2 x=floor(v.xz);"
             "v.xz-=x+.5;"
             "v.y+=2.;"
             "vec3 k=vec3(.3,.02,.3);"
             "float t=-d(v.xz,vec2(.7));"
             "return min(t,h(v,k));"
           "}"
   "return h(a(m/10.)*p(m/20.)*v,vec3(1.));"
 "}"
 "vec3 o(vec3 v)"
 "{"
   "return normalize(vec3(D(v+i.yxx),D(v+i.xyx),D(v+i.xxy))-D(v));"
 "}"
 "float b=1./v.x;"
 "float D(vec3 v,vec3 m,float y,float x,int i)"
 "{"
   "for(int f=0;f<i;++f)"
     "{"
       "float g=D(v+m*y);"
       "y+=g;"
       "if(g<b*y||y>x)"
         "break;"
     "}"
   "return y;"
 "}"
 "float a(vec2 v,float m,float y,float i,float f)"
 "{"
   "float g=length(v);"
   "f+=atan(v.x,v.y)/x;"
   "return step(m,g)*step(g,y)*step(.5,mod(f*i,2.));"
 "}"
 "vec3 C,k,w=vec3(.01);"
 "vec4 S(int v)"
 "{"
   "g=v;"
   "float m=D(C,k,0.,10.,64);"
   "if(m<10.)"
     "{"
       "vec3 y=C+k*m,f=o(y),i=vec3(1.);"
       "if(g==1)"
         "i=mix(vec3(1.),vec3(1.,.2,.3),step(u,z));"
       "else"
         " if(g==4)"
           "f=o((floor(y*4.)+.5)/4.);"
         "else"
           " if(g==5)"
             "i=vec3(.1,.2,.9);"
       "vec3 x=-k;"
       "return vec4(i*(w+max(0.,dot(f,x))),1.);"
     "}"
   "return vec4(0.);"
 "}"
 "vec2 D(vec2 v,vec4 m)"
 "{"
   "return((v-m.xy)/m.zw-.5)*2.;"
 "}"
 "void main()"
 "{"
   "vec2 y=gl_FragCoord.xy,f=y-v/2.,i=gl_FragCoord.xy/v*2.-1.;"
   "i.x*=v.x/v.y;"
   "C=vec3(0.,0.,5.);"
   "k=normalize(vec3(i,-2.));"
   "vec3 g=vec3(0.);"
   "vec4 x=S(5);"
   "g=mix(g,x.xyz,x.w);"
   "g+=texture2D(T,(gl_FragCoord.xy-vec2(0,m*16.))/2./textureSize(T,0)).xyz;"
   "g+=.1*vec3(1.-fract((m-4.)/8.));"
   "gl_FragColor=vec4(g,1.);"
 "}";

#endif // INTRO_H_

/* File generated with Shader Minifier 1.1.4
 * http://www.ctrl-alt-test.fr
 */
#ifndef INTRO_H_
# define INTRO_H_

const char *intro_glsl =
 "#version 130\n"
 "uniform vec2 RES;"
 "uniform float t;"
 "uniform sampler2D N;"
 "const vec3 v=vec3(0.,.001,1.);"
 "const float m=3.14159;"
 "float f=t/8.,l=t/16.,x=t/32.,y=t/64.;"
 "float n(float v)"
 "{"
   "return fract(sin(v)*43758.5);"
 "}"
 "float p(vec2 v)"
 "{"
   "return n(dot(v,vec2(129.47,37.56)));"
 "}"
 "float e(vec2 f)"
 "{"
   "vec2 m=floor(f);"
   "f=fract(f);"
   "return mix(mix(p(m),p(m+v.zx),f.x),mix(p(m+v.xz),p(m+v.zz),f.x),f.y);"
 "}"
 "float r(vec2 v)"
 "{"
   "return.5*e(v)+.25*e(v*2.3)+.125*e(v*4.1);"
 "}"
 "vec4 s(vec2 v)"
 "{"
   "return texture2D(N,v);"
 "}"
 "vec4 d(vec2 v)"
 "{"
   "return texture2D(N,(v+.5)/textureSize(N,0));"
 "}"
 "float d(vec3 v,vec3 x)"
 "{"
   "return v=abs(v)-x,max(v.x,max(v.y,v.z));"
 "}"
 "vec3 e(vec3 v,vec3 f)"
 "{"
   "return mod(v,f)-f*.5;"
 "}"
 "mat3 h(float v)"
 "{"
   "float f=sin(v),x=cos(v);"
   "return mat3(1.,0.,0.,0.,x,-f,0.,f,x);"
 "}"
 "mat3 R(float v)"
 "{"
   "float f=sin(v),x=cos(v);"
   "return mat3(x,0.,f,0.,1.,0,-f,0.,x);"
 "}"
 "float R(float v,float f,float x)"
 "{"
   "return max(x,min(v,f))-length(max(vec2(x-v,x-f),vec2(0)));"
 "}"
 "mat3 d(vec3 v,vec3 f,vec3 x)"
 "{"
   "vec3 m=normalize(v-f),y=normalize(cross(x,m));"
   "x=normalize(cross(m,y));"
   "return mat3(y,x,m);"
 "}"
 "float z=6.36e+06,i=6.38e+06,S=40.;"
 "vec3 k=vec3(0.,-z,0.),c=vec3(5.8e-06,1.35e-05,3.31e-05),a=vec3(2e-05),o=a*1.1,u;"
 "vec2 D(vec3 v)"
 "{"
   "float f=max(.1,length(v-k)-z);"
   "return vec2(exp(-f/8000.),exp(-f/1200.));"
 "}"
 "float D(vec3 v,vec3 f,float x)"
 "{"
   "vec3 m=v-k;"
   "float e=dot(m,f),y=dot(m,m)-x*x,i=e*e-y;"
   "if(i<0.)"
     "return-1.;"
   "i=sqrt(i);"
   "float n=-e-i,u=-e+i;"
   "return n>=0.?n:u;"
 "}"
 "vec2 D(vec3 v,vec3 f,float m,float x)"
 "{"
   "vec2 e=vec2(0.);"
   "m/=x;"
   "f*=m;"
   "for(float i=0.;i<x;++i)"
     "e+=D(v+f*i);"
   "return e*m;"
 "}"
 "vec3 R(vec3 v,vec3 f,float m,vec3 x)"
 "{"
   "vec2 e=vec2(0.);"
   "vec3 r,y;"
   "r=y=vec3(0.);"
   "float z=dot(f,u),n=8.;"
   "m/=n;"
   "f*=m;"
   "for(float k=0.;k<n;++k)"
     "{"
       "vec3 E=v+f*k;"
       "vec2 s=D(E)*m;"
       "e+=s;"
       "vec2 R=e+D(E,u,D(E,u,i),8.);"
       "vec3 d=exp(-c*R.x-o*R.y);"
       "r+=d*s.x;"
       "y+=d*s.y;"
     "}"
   "return x*exp(-c*e.x-o*e.y)+S*(1.+z*z)*(r*c*.0597+y*a*.0196/pow(1.58-1.52*z,1.5));"
 "}"
 "float g(vec3 f)"
 "{"
   "return f.y+=.2,min(length(f-v.zxx*1.4),length(f+v.zxx*1.4))-.8;"
 "}"
 "vec3 E=vec3(8.,4.,8.);"
 "float b=0.;"
 "float w(vec3 v)"
 "{"
   "v.z-=4.;"
   "float f=-d(v,E);"
   "if(t>768.)"
     "f=max(max(f,d(v,E+.3)),-d(e(v-u*E,vec3(7.,9.,2.)),b*vec3(3.,4.,.5)));"
   "return f;"
 "}"
 "mat3 C=h(y)*R(x);"
 "float T(vec3 v)"
 "{"
   "return d(C*(v-vec3((t-320.)*3./256.-1.5,-.2,0.)),vec3(.3));"
 "}"
 "float q(vec3 v)"
 "{"
   "float f=g(v);"
   "if(t>320.&&t<576.)"
     "f=R(f,T(v),.3);"
   "return min(w(v),f);"
 "}"
 "vec3 Z(vec3 f)"
 "{"
   "return normalize(vec3(q(f+v.yxx)-q(f-v.yxx),q(f+v.xyx)-q(f-v.xyx),q(f+v.xxy)-q(f-v.xxy)));"
 "}"
 "float D(vec3 v,vec3 f,float x,float m,int y)"
 "{"
   "for(int i=0;i<y;++i)"
     "{"
       "float n=q(v+f*x);"
       "x+=n;"
       "if(n<.001*x||x>m)"
         "break;"
     "}"
   "return x;"
 "}"
 "void main()"
 "{"
   "vec2 f=gl_FragCoord.xy/RES*2.-1.;"
   "f.x*=2.*RES.x/RES.y;"
   "vec3 m=vec3(0.,1.5,3.9),x=v.xzx,y=vec3(0.),e=normalize(vec3(f,-2.));"
   "if(t<384.)"
     "{"
       "float z=smoothstep(64.,384.,t);"
       "y=mix(vec3(0.,.4,-4.),vec3(0.),z);"
       "m=mix(vec3(0.,.4,-3.),vec3(0.,1.5,3.9),z);"
     "}"
   "else"
     "{"
       "float z=smoothstep(432.,640.,t);"
       "m=mix(m,vec3(0.,-.2,5.9),z);"
       "float k=smoothstep(512.,704.,t);"
       "E=mix(E,vec3(2.4,3.,6.),k);"
       "b=mix(.38,1.,smoothstep(768.,896.,t));"
     "}"
   "e=d(m,y,x)*e;"
   "u=normalize(mix(vec3(0.,10.,0.),vec3(-.2,.01,-.3),t/1216.));"
   "float z=t;"
   "vec3 s=vec3(0.);"
   "for(int k=0;k<16;++k)"
     "{"
       "vec3 c=m,a=e,l=vec3(0.),p=vec3(1.);"
       "for(int S=0;S<4;++S)"
         "{"
           "float h=D(c,a,i);"
           "int q=0;"
           "float C=h,o=a.y<-.0001?(-1.-c.y)/a.y:C;"
           "if(o<C)"
             "C=o,q=1;"
           "float T=min(C,40.),Y=D(c,a,0.,T,32);"
           "if(Y<T)"
             "C=Y,q=2;"
           "vec3 X=vec3(0.),W=vec3(.75,.75,.73),V=vec3(0.,1.,0.);"
           "float U=.04;"
           "if(q==0)"
             "{"
               "X=R(c,a,C,vec3(0.));"
               "if(any(isnan(X)))"
                 "break;"
               "W=vec3(0.);"
               "c=c+a*C;"
             "}"
           "else"
             "{"
               "c=c+a*C;"
               "if(q==1)"
                 "U=.01+.6*smoothstep(.4,.5,r(c.xz*2.));"
               "else"
                 " if(q==2)"
                   "{"
                     "V=Z(c);"
                     "c+=V*.01;"
                     "if(g(c)>w(c))"
                       "{"
                         "U=.8;"
                         "if(V.z>.9)"
                           "{"
                             "vec2 Q=floor(c.xy*4.);"
                             "vec4 P=d(Q.yy*4.);"
                             "float O=mod(-t/2.+P.x*64.,64.)-32.;"
                             "X=vec3(step(O,Q.x)*step(Q.x,O+1.+8.*P.y)*step(Q.x,(-t+734.)/2.));"
                           "}"
                       "}"
                   "}"
             "}"
           "l+=p*X;"
           "p*=W.xyz;"
           "if(all(lessThan(p,vec3(.001))))"
             "break;"
           "a=reflect(a,V);"
           "a=normalize(mix(a,(vec3(n(z+=a.x),n(z+=a.y),n(z+=a.z))-.5)*2.,U));"
           "a*=sign(dot(V,a));"
         "}"
       "s+=l;"
     "}"
   "gl_FragColor=vec4(s/16.,.3);"
 "}";

#endif // INTRO_H_
